function devolucion() {
    var input = prompt(`¿Que problemas ha tenido con su devolucion?`);
    if (input === "") {
        alert(`Debe de describir cual es su problema para poder ayudarle.`);
    } else {
        alert(`Gracias por crear una incidencia, contactaremos con usted lo antes posible.`)
    }
}
function garantia() {
    var input = prompt(`¿Cual es el problema resspecto a la garantía?`);
    if (input === "") {
        alert(`Debe de describir cual es su problema para poder ayudarle.`);
    } else {
        alert(`Gracias por crear una incidencia, contactaremos con usted lo antes posible.`)
    }
}
function regalo() {
    var input = prompt(`¿Que problema ha tenido con su regalo?`);
    if (input === "") {
        alert(`Debe de describir cual es su problema para poder ayudarle.`);
    } else {
        alert(`Gracias por crear una incidencia, contactaremos con usted lo antes posible.`)
    }
}
function envio() {
    var input = prompt(`¿Cual ha sido el problema con el envio?`);
    if (input === "") {
        alert(`Debe de describir cual es su problema para poder ayudarle.`);
    } else {
        alert(`Gracias por crear una incidencia, contactaremos con usted lo antes posible.`)
    }
}
function atencion() {
    var input = prompt(`¿Cual es su problema con la atencion al cliente?`);
    if (input === "") {
        alert(`Debe de describir cual es su problema para poder ayudarle.`);
    } else {
        alert(`Gracias por crear una incidencia, contactaremos con usted lo antes posible.`)
    }
}